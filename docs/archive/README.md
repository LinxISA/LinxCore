# LinxCore Documentation Archive

Everything below this directory is historical, non-normative material. Active
LinxCore implementation and architecture documentation lives under
`docs/architecture/`.

Archived snapshots retain the spellings and classifications that were current
when they were captured. They must not be consumed by generators, parity gates,
or implementation code. Each archived release directory carries an
`archive.yaml` record describing its provenance and replacement.
